_schema-version: 3.3.0
ID: EMPLOYEE1-CAP
description: A simple CAP project.
version: 1.0.0
modules:
  - name: EMPLOYEE1-CAP-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: EMPLOYEE1-CAP-db
      - name: employee1-cap-auth
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: EMPLOYEE1-CAP-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: EMPLOYEE1-CAP-db
  - name: EMPLOYEE1-CAP-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: EMPLOYEE1-CAP-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - demoempsal.zip
          name: demoempsal
          target-path: resources/
  - name: demoempsal
    type: html5
    path: app/sal
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
  - name: employee1-cap
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}
resources:
  - name: EMPLOYEE1-CAP-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: EMPLOYEE1-CAP-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: EMPLOYEE1-CAP-html5-service
      service-plan: app-host
  - name: EMPLOYEE1-CAP-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: EMPLOYEE1-CAP-destination-service
      service-plan: lite
  - name: employee1-cap-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: employee1-cap-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
